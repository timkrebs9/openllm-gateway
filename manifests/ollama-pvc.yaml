# filepath: c:\Development\openllm-gateway\manifests\ollama-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-deepseek-data-pvc
spec:
  accessModes:
    - ReadWriteOnce # Suitable for single pod access
  resources:
    requests:
      storage: 20Gi # deepseek-r1:8b is ~8GB, allow space for others/cache
  # storageClassName: <your-aks-storage-class> # Specify if needed, e.g., 'managed-csi' or 'azurefile-csi'